# OrderRail Environment Variables
#
# Copy this file to .env for local development:
#   cp .env.example .env
#
# These values are safe defaults for production-mode local development.
# To test demo mode locally, change VITE_DEMO_MODE to "true".
#
# On Vercel, set these in Settings → Environment Variables for each project.
# See docs/DEMO_MODE.md for full details.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key
VITE_DEMO_MODE=false
# Identifies which cafe record to load from the database by slug (defaults to "orderrail")
VITE_CAFE_SLUG=your-cafe-slug
